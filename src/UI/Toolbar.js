export class Toolbar { constructor(root,onMode){ const modes=['brand','price','thc','terpenes','effects']; this.el=document.createElement('div'); this.el.className='toolbar'; this.el.innerHTML=modes.map((m,i)=>`<button class="${i?'':'active'}" data-mode="${m}">${m}</button>`).join(''); root.append(this.el); this.el.onclick=e=>{ if(e.target.dataset.mode){ this.el.querySelectorAll('button').forEach(b=>b.classList.toggle('active',b===e.target)); onMode(e.target.dataset.mode); }}; }}
